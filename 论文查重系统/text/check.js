const assert = require('assert');

const sum = require('../index');

describe('#index.js', () => {

    // it('sum() should return 0', () => {
    //     assert.strictEqual(sum(), 0);
    // });

});
//待实现：单元测试
